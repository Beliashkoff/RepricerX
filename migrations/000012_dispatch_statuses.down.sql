-- PostgreSQL не поддерживает удаление значений из ENUM в безопасном виде:
-- DROP/RENAME TYPE требует пересоздания всех колонок, использующих тип,
-- что небезопасно для production-данных и связанных индексов/триггеров.
--
-- Down-миграция оставлена пустой намеренно. Откат Этапа 6 возможен только
-- логическим: удалением кода, использующего новые значения. Сами значения
-- ENUM остаются — они инертны и не мешают предыдущим версиям приложения.
